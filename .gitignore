# ⚠️ 本地密钥文件——绝不能提交到仓库（DeepSeek key / 入口码）
# 这是本仓库最重要的安全防线：key 一旦进 git 历史，删除无效，必须吊销重发
.dev.vars

# 依赖与日志
node_modules/
*.log
.DS_Store

# wrangler dev 本地缓存（Miniflare 状态与临时产物）
.wrangler/

# 测试临时产物（cases.md 跑批的请求/响应 JSON）
tests/tmp_results/

# 教练经验随记本——带教经验是产品壁垒，只留在本地给 AI 消化（仓库公开的只有提炼后的 prompt.js）
docs/
